-- mason.nvim is a master-only plugin (Android termux has no usable LSP binaries
-- from mason). This file is an empty stub on the termux branch so that
-- `.gitattributes merge=ours` keeps it inert across future master->termux merges
-- without touching master's real mason.lua.
return {}
